# N243 — citizens.yaml
# Profil: CITIZEN / L4
version: "1.0"
intent_hash: "0xN243_CITIZENS_20260801"

repo:
  name: "N243"
  stratum: "L4-TOOLS"
  status: "active"
  description: "Meta-orchestrateur cognitif L*"

citizens:
  - name: "Orchestrator"
    role: "orchestrator"
    description: "Orchestre les runners L* via le bus WAZAA"
    phase: "orchestration"
  
  - name: "TernaryGuardian"
    role: "validator"
    description: "Valide les décisions ternaires (Approve/Suspend/Reject)"
    phase: "validation"
  
  - name: "BDCPEnforcer"
    role: "enforcer"
    description: "Applique BDCP sur les clones et forks"
    phase: "governance"

  # PRD-MOC-N243-SOVEREIGN-CROSS-REPO-GRAPH-2026-08-04 citizens
  - name: "n243-builder"
    role: "builder"
    description: "Construit et maintient le graphe souverain N243"
    phase: "ingestion"

  - name: "n243-query"
    role: "query"
    description: "Exécute les requêtes TQL sur le graphe N243 via CTULU"
    phase: "query"

  - name: "mox-validator"
    role: "validator"
    description: "Garantit la méta-cohérence cross-repo via PLIX + KIX + NEXUS"
    phase: "validation"

  - name: "auto-dev-orchestrator"
    role: "orchestrator"
    description: "Orchestre les cycles RAI (KEEL, META-LOOP, champion, PR factory)"
    phase: "orchestration"

  # PRD-MOC-GEN-082 automation primitives
  - name: "repo-sanitizer"
    role: "sanitizer"
    description: "Nettoie les working trees, supprime les caches sales, formate les YAML"
    phase: "session-end"

  - name: "test-runner-adaptive"
    role: "runner"
    description: "Lance pytest avec timeout dynamique, parse les résultats, isole les échecs"
    phase: "post-modif"

  - name: "sdk-compatibility-checker"
    role: "checker"
    description: "Vérifie que les imports d'un repo correspondent aux SDK installés"
    phase: "pre-commit"

  - name: "nexus-validator"
    role: "validator"
    description: "Vérifie la conformité des frontmatter et noms de fichiers NEXUS"
    phase: "pre-commit"

  - name: "drift-classifier"
    role: "classifier"
    description: "Classe les drifts en legitimate/noise/blocking avant commit"
    phase: "session-end"
